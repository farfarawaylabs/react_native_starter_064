import React from 'react';
import {Text} from 'react-native';
import {Center} from '@farfarawaylabs/react-native-easy-flex';

const HomeScreen = () => {
  return (
    <Center vertical horizontal style={{width: '100%', height: '100%'}}>
      <Text>Login</Text>
    </Center>
  );
};

export default HomeScreen;
